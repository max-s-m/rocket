// Приклад програми мовою Rocket
// Демонстрація всіх токенів, типів і операторів

// Глобальні оголошення змінних з усіма типами
int a = 0;
const int b = 1;
int aa = 1;
float pi = 3.14;
bool ok = true;
string name = "Rocket";

// Арифметика та логіка
a = 5 + 3*2 - 1 ^ 2 ^ 3;
pi = -(-12 / 2.0) ^ 3 * 4;          // степінь через ^
ok = !false && true || false;

a += 10;
a -= 2;
a *= 2;
a /= 2;

// Тест ідентифікаторів з підкресленням
int my_var1 = 123;
float my_var2 = 0.5;

// Виведення та введення даних
print("This is a: ", a);
print("Enter int");
a = inputInt();
print(a);
print("Enter float");
pi = inputFloat();
print(pi);
print("Enter bool");
ok = inputBool();
print(ok);
print("Enter string");
name = inputString();
print(name);

// Порівняння та тернарний оператор
bool gt = (a > 0);
bool ge = (a >= 0);
bool lt = (a < 0);
bool le = (a <= 0);
bool eq = (a == 0);
bool ne = (a != 0);
int sign = (a > 0) ? 1 : -1;

// Дужки й пунктуація
a = (a + 1);
pi = pi;
{ } // фігурні дужки як спецсимволи

//функція
int myfunc(int a, int b){
print("This is func");
int c = a + b;
return c;
}

void check_a(){
print("Hello, current a value:", a);
}

check_a();
int res = myfunc(a, 2);
print("Myfunc res:", res);

// if / elif / else
if (a > 0) {
    print("Positive");
} elif (a == 0) {
    print("Zero");
} else {
    print("Negative");
}

// switch / case / default
switch a {
    case 1:
        print("One");
        break;
    case 2:
        print("Two");
        break;
    default:
        print("Other");
}

// while
while (a < 3) {
    print("While loop");
    a = a + 1;
}

// do ... while
do {
    print("Do-While loop");
    a = a - 1;
} while (a > 0);

// for
for (int i = 1; i < 3; i = i + 1) {
    print("For");
}

// Кінець файлу